$("#comments_<%= @post.id %>").html("<%= j render partial: 'comments/index', locals: { post: @post } %>");
$('.notification_unread_count').html('<li><%= j pluralize(Notification.unread_count(current_user), '') %></li>');