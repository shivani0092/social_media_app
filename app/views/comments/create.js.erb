$("#comments_<%= @post.id %>").html("<%= j render partial: 'comments/index', locals: { post: @post } %>");
$(".create-comments").html("<%= j render partial: 'comments/create', locals: { post: @post } %>");
